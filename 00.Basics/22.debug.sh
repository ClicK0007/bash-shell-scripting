## 1. use "bash -x _file_name_" in command to debug.
## 2. at start use, "#!/bin/bash -x"
## 3. In program use, "set -x" then from that debugging starts.
## 3. In program use, "set +x" then from that debugging ends.